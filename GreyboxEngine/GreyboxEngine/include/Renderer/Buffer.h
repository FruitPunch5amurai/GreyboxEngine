#pragma once
namespace GreyboxEngine
{
    struct VertexArrayHandle {unsigned int id;};
    struct VertexBufferHandle {unsigned int id;};
    struct IndexBufferHandle {unsigned int id;};

    struct ShaderProgramHandle {unsigned int id;};
}
